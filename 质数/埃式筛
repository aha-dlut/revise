求小于n的所有质数
对于2， 2是质数，所以4不是质数，6，8，10....等等均不为质数，因为使用了倍数因子
埃式筛也就是对于2 3 5等质数求倍数，然后删除他们，剩下的数字即为质数

注意：只需要求到根号n的位置即可

const int n = 100000;
bool vis[n] ={true,true}//默认0和1都不是质数


for(int i = 2; i*i < n; i++){
  //如果此时的是质数 那么就不断倍增删除其倍数
  if（！vis[i]）{
      for(int j = i*2; j <= n; j++){
        vis[j] == true;//j不是质数
      }
  }
  
}

上面会有重复筛的情况，比如2*3和3*2就是重复筛，影响效率，为nlogn
解决该问题可以每次j不从i*2开始，而是从i*i开始

或者使用欧拉筛 时间复杂度为n
